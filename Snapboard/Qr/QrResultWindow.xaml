<Window x:Class="Snapboard.Qr.QrResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Snapboard — QR scan result"
        Icon="/Assets/snapboard.ico"
        Width="620" Height="460"
        MinWidth="420" MinHeight="280"
        WindowStartupLocation="CenterScreen"
        Background="#FF15171C"
        Foreground="#FFE6E8EC"
        FontFamily="Segoe UI"
        UseLayoutRounding="True"
        Focusable="True"
        PreviewKeyDown="OnPreviewKeyDown">
    <Window.Resources>
        <SolidColorBrush x:Key="CardBg"      Color="#FF1C1F26"/>
        <SolidColorBrush x:Key="CardBorder"  Color="#FF2A2E37"/>
        <SolidColorBrush x:Key="Muted"       Color="#FF9BA1AD"/>
        <SolidColorBrush x:Key="Accent"      Color="#FF8B5CF6"/>
        <SolidColorBrush x:Key="AccentHover" Color="#FFA78BFA"/>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="#FF2A3240"/>
                                <Setter Property="Foreground" Value="#FF5B6170"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource Muted}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#22FFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="22,18,22,10">
            <StackPanel Orientation="Horizontal">
                <Border Background="#3A8B5CF6" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                    <TextBlock Text="&#x25A3;" Foreground="#FFC4B5FD" FontSize="14"/>
                </Border>
                <TextBlock Text="QR code detected"
                           FontSize="20" FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock x:Name="MetaText"
                       Foreground="{StaticResource Muted}"
                       FontSize="12" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1"
                Background="{StaticResource CardBg}"
                BorderBrush="{StaticResource CardBorder}"
                BorderThickness="1"
                CornerRadius="8"
                Margin="22,0,22,10">
            <TextBox x:Name="ResultBox"
                     Background="Transparent"
                     Foreground="#FFE6E8EC"
                     CaretBrush="White"
                     BorderThickness="0"
                     Padding="14"
                     FontSize="14"
                     FontFamily="Consolas"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     IsReadOnlyCaretVisible="True"/>
        </Border>

        <Border Grid.Row="2"
                Background="#FF12141A"
                BorderBrush="{StaticResource CardBorder}"
                BorderThickness="0,1,0,0"
                Padding="22,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           x:Name="StatusText"
                           Foreground="{StaticResource Muted}"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Text="Esc to close"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Style="{StaticResource GhostButton}" Content="Close" Click="OnCloseClick" Margin="0,0,8,0"/>
                    <Button x:Name="OpenButton"
                            Style="{StaticResource GhostButton}"
                            Content="Open link"
                            Visibility="Collapsed"
                            Margin="0,0,8,0"
                            Click="OnOpenClick"/>
                    <Button x:Name="CopyButton"
                            Style="{StaticResource PrimaryButton}"
                            Content="Copy"
                            Click="OnCopyClick"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
