<Window x:Class="Snapboard.Ruler.PixelRulerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Snapboard Ruler"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        Width="900" Height="72"
        MinWidth="200" MinHeight="40"
        Focusable="True"
        MouseLeftButtonDown="OnWindowMouseLeftButtonDown"
        MouseLeftButtonUp="OnWindowMouseLeftButtonUp"
        MouseMove="OnWindowMouseMove"
        MouseLeave="OnWindowMouseLeave"
        KeyDown="OnKeyDown">
    <Window.Resources>
        <!-- Dark context menu that matches the rest of the app.
             Background/Foreground alone aren't enough — the default WPF system
             template paints its own light chrome. We override Template here. -->

        <!-- Separator inside the menu. -->
        <Style x:Key="DarkMenuSeparator" TargetType="Separator">
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="6,4,6,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Background="#FF2A2E37" Height="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Flat MenuItem template with checkmark column. Works for plain and
             checkable items (we don't use submenus in the ruler menu). -->
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="#FFE6E8EC"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid>
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="3"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="18"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Path x:Name="Check"
                                          Grid.Column="0"
                                          Data="M 0,4 L 3,7 L 9,1"
                                          Stroke="#FF3DA9FC"
                                          StrokeThickness="1.8"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Visibility="Collapsed"/>
                                    <ContentPresenter Grid.Column="1"
                                                      ContentSource="Header"
                                                      RecognizesAccessKey="True"
                                                      VerticalAlignment="Center"/>
                                    <Path x:Name="Arrow"
                                          Grid.Column="2"
                                          Data="M 0,0 L 4,4 L 0,8 Z"
                                          Fill="#FF9BA1AD"
                                          Margin="14,0,2,0"
                                          VerticalAlignment="Center"
                                          Visibility="Collapsed"/>
                                </Grid>
                            </Border>

                            <!-- Submenu popup, reused for SubmenuHeader items. -->
                            <Popup x:Name="PART_Popup"
                                   Placement="Right"
                                   HorizontalOffset="-2"
                                   VerticalOffset="-4"
                                   IsOpen="{TemplateBinding IsSubmenuOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="None">
                                <Border Background="#FF1C1F26"
                                        BorderBrush="#FF2A2E37"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="4"
                                        SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Role" Value="SubmenuHeader">
                                <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Role" Value="TopLevelHeader">
                                <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF2A3340"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#FF6B7280"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Override the ContextMenu chrome entirely. -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="#FF1C1F26"/>
            <Setter Property="BorderBrush" Value="#FF2A2E37"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FFE6E8EC"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="HasDropShadow" Value="False"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Settings gear button — minimal, quiet until hovered. -->
        <Style x:Key="GearButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#66000000"/>
            <Setter Property="Foreground" Value="#FFE6E8EC"/>
            <Setter Property="BorderBrush" Value="#FF2E3340"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#CC000000"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#FF3DA9FC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Opaque backing so hit-testing works across the whole ruler surface -->
        <Border x:Name="Root"
                Background="#EE12141A"
                BorderBrush="#FF3DA9FC"
                BorderThickness="1"
                CornerRadius="2"/>

        <!-- Ruler tick marks / labels / cursor indicator. IsHitTestVisible=False
             so mouse events fall through to the Window handlers for drag/resize. -->
        <Canvas x:Name="RulerCanvas"
                IsHitTestVisible="False"
                SizeChanged="OnCanvasSizeChanged"/>

        <!-- Settings gear. Position is updated in code to match orientation. -->
        <Button x:Name="GearButton"
                Style="{StaticResource GearButtonStyle}"
                Click="OnGearClick"
                Content="⚙"
                ToolTip="Ruler settings (right-click also opens menu)"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Margin="3,0,0,0"/>
    </Grid>
</Window>
