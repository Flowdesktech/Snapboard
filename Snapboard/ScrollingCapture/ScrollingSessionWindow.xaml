<Window x:Class="Snapboard.ScrollingCapture.ScrollingSessionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Snapboard — Scrolling capture"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        Focusable="False"
        ShowActivated="False"
        SizeToContent="WidthAndHeight"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        MouseDown="OnWindowMouseDown"
        Loaded="OnLoaded"
        Closed="OnClosed">
    <Window.Resources>
        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Foreground" Value="#FFE6E8EC"/>
            <Setter Property="Background" Value="#22FFFFFF"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="Card"
            Background="#EE12141A"
            BorderBrush="#33FFFFFF"
            BorderThickness="1"
            CornerRadius="12"
            Padding="14,12"
            MinWidth="300">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="24" ShadowDepth="2" Opacity="0.55"/>
        </Border.Effect>
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Ellipse Grid.Column="0"
                         x:Name="RecDot"
                         Width="10" Height="10"
                         Fill="#FFEF4444"
                         VerticalAlignment="Center"
                         Margin="0,0,10,0"/>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Scrolling capture"
                               Foreground="White"
                               FontSize="13"
                               FontWeight="SemiBold"/>
                    <TextBlock x:Name="StatusText"
                               Text="Auto-scrolling target window…"
                               Foreground="#FFB6BCC8"
                               FontSize="11"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0"
                        Background="#22FFFFFF"
                        CornerRadius="10"
                        Padding="9,3">
                    <TextBlock x:Name="FrameCountText"
                               Text="0 frames"
                               Foreground="#FFE6E8EC"
                               FontSize="11"/>
                </Border>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Style="{StaticResource GhostButton}"
                            Content="Stop &amp; save"
                            Margin="0,0,8,0"
                            Click="OnStopClick"/>
                    <Button Style="{StaticResource GhostButton}"
                            Content="Cancel"
                            Click="OnCancelClick"/>
                </StackPanel>
            </Grid>

            <TextBlock Foreground="#FF8C93A3"
                       FontSize="10"
                       Margin="0,8,0,0"
                       TextWrapping="Wrap"
                       MaxWidth="320">
                Snapboard is sending wheel events to the target window and auto-stitching frames. It stops on its own when the end of the page is reached, or click Stop &amp; save to finish early.
            </TextBlock>
        </StackPanel>
    </Border>
</Window>
